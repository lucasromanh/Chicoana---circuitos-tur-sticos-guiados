import React from 'react';
import { View, Text } from 'react-native';

export default function AppNative() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Chicoana Turismo (Native)</Text>
        </View>
    );
}
